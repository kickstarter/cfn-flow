---
service: cfn-flow-specs
region: us-east-1
templates:
  s3_bucket: test-bucket
  s3_prefix: test-prefix

stack:
  stack_name: cfn-flow-spec-<%= Time.now.to_i %>
  template_body: sqs.yml
  parameters:
    AlarmEMail: test@example.com
  tags:
    test_tag: hello world
